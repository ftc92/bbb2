@model MvcApplication4.Models.Hall

@{
    var a = Model;
    ViewBag.Title = "Details";
}

<h1>@a.Name   </h1>
<h2>Booking  </h2>

<table >
    <tr><th>Hall</th><<th>Date</th><th>People</th><th>Status</th></tr>
    @foreach (var h in a.h)
    {
        foreach (var b in h.Bookings) { 

        <tr><td>@b.h.name</td><<td>@b.date</td><td>@b.people</td><td>@b.status</td><td>@Html.ActionLink("Approve", "Approve", new {id=@b.Id})</td><td>@Html.ActionLink("Cancel", "Cancel", new {id=@b.Id})</td><td>@Html.ActionLink("Delete", "Delete", new {id=@b.Id})</td></tr>


    }
    }
    
</table>

<table>
    <tr>
        <td> 
            <h2>@Html.ActionLink("Add/Delete Halls", "Index", "Halls2")              </h2>
            <h2>Promotions              </h2>
           
            @foreach (var s in a.Promotions)
            {
                <b style="color:hotpink">@s.Desc</b>  



            }
            @*<h2>Available Halls </h2>
            Select Date:
            <form method="post" action="/Halls/book">
                <input type="date" name="Date" id="Date" />
                <input type="hidden" value="@a.Id" name="id" />

                <input type="submit" class="btn btn-info" value="Check" />
            </form>*@



            @*@if (@ViewBag.list != null)
            {
                foreach (var s in @ViewBag.list)
                {
                    <label> @s.name    <b style="color:green">available    @Html.ActionLink("Book now", "book", new { id = s.Id })</b></label>


                }
            }*@
        
       
            <h2> Videos </h2>

            @foreach (var s in a.Videos)
            {
                //<iframe width="420" height="315" src=@s.Link> </iframe>
                //   <iframe width="560" height="315" src="https://www.youtube.com/embed/AdyC1LJ2pWM" frameborder="0" allowfullscreen></iframe>
                <iframe width="560" height="315" src="@s.Link" frameborder="0" allowfullscreen></iframe>;

                //WebUtility.HtmlEncode(@s.Link);

                <div>

                </div>
            }

        </td>

    </tr>
</table>
@*<script src="../../Scripts/jquery-1.3.2.min.js" language="javascript" type="text/javascript"></script>
    <script src="../../Scripts/jquery-ui-1.7.1.custom.min.js" type="text/javascript"></script>*@